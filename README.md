# ToDoList-FullStack
A simple and modern To-Do web application that allows multiple users to **register, log in, and manage their personal tasks**.   The app supports **CRUD operations** (Create, Read, Update, Delete) and is built using **Next.js (frontend)** and **Flask (backend)**.
